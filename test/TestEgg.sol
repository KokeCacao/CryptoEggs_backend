pragma solidity >=0.8.12 <0.9;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Egg.sol";

contract TestEgg {
  function test() public {


    // Egg egg = Egg(DeployedAddresses.Egg());
    // uint256 i = 1;
    // Assert.equal(i, 1, "The world as we know it has ended!");
    // address addr = address(0); // make sure the type of literal is correct
    // Assert.equal(egg.getEggOwner(0), addr, "Nobody should have egg");
  }
}
